{% block navbar_script %}
    <script>
        const navItems = [
            {% for item in nav_items %}
            {
                id: {{ item.id }},
                name: "{{ item.name }}",
                link: "{{ item.link }}"
            },
            {% endfor %}
        ]
    </script>
{% endblock %}